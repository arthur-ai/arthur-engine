__pycache__/
*.py[cod]
.venv/
.env
.env.local
*.egg-info/
.pytest_cache/
.mypy_cache/
# Runtime HF weight cache (MODEL_STORAGE_PATH=/models in the container,
# ./models in baremetal dev). Anchored — must NOT match src/models/, which
# is the loader/downloader/registry source.
/models/
