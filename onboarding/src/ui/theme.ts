import chalk from 'chalk';

export const theme = {
  primary:    chalk.cyan,
  success:    chalk.green,
  warning:    chalk.yellow,
  error:      chalk.red,
  dim:        chalk.dim,
  bold:       chalk.bold,
  astronaut:  chalk.cyan.bold,
  step:       chalk.cyan.dim,
};
