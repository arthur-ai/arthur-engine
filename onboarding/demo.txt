Steps to test:
git clone https://github.com/langchain-ai/agents-from-scratch
cd agents-from-scratch

API keys needed: OPENAI_API_KEY (required)
LANGSMITH_API_KEY (optional, can skip)

When Buzz asks you to run the agent for trace verification (Step 7), run:
uv run python -c "
from src.email_assistant.email_assistant import graph
result = graph.invoke({'messages': [{'role': 'user', 'content': 'hello'}]})
print(result)
"
