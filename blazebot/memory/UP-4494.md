# Session Memory — UP-4494

## Progress
- Sessions 1-4: Attempted incremental fixes (SVG→box-shadow, inset shorthand, keyframe opacity ramp)
- Session 5: PR reviewer rejected incremental approach — produced full rework plan
- Session 6 (this): Implemented the full architectural rework per reviewer's spec:
  - Removed `getPulseExpansionGeometry` helper, `pulseSpread`, CSS custom properties (`--task-tour-pulse-inset`, `--task-tour-pulse-radius`)
  - Replaced inset+var() animation with transform: scale() + opacity keyframes
  - Static ring remains as box-shadow (never animated)
  - Ripple layer is ::after pseudo-element using only GPU-composited properties
  - Keyframes use literal numeric scale values (no var(), no inherit)
  - Rewrote test to assert transform/opacity animation and absence of CSS custom properties
  - All checks pass: tests, type-check (clean for our files), lint, format

## Decisions Made
- Used `spread = 14` (per plan) for scale factor calculation
- Scale factors computed as `((width + spread * 2) / width).toFixed(4)` for X and Y
- Kept 70%-100% hold at opacity 0 to prevent visible loop wrap-around
- Used `inset: 0` on ::after (shorthand is fine for static value, only var() in keyframes was the Safari issue)
- Test uses non-trivial DOMRect(100, 50, 300, 40) so scale values are distinct for X/Y

## Blockers
- None

## Files Touched
- `genai-engine/ui/src/features/task-tour/components/PulsingRing.tsx` — full rewrite
- `genai-engine/ui/src/features/task-tour/components/PulsingRing.test.tsx` — full rewrite

## Prior Sessions
- Session 1: Research identified two root causes (SVG mask, inset shorthand)
- Session 2: Implemented SVG→box-shadow and inset expansion fixes
- Session 3: Identified remaining flicker issue per PR reviewer feedback, produced fix plan
- Session 4: Applied keyframe fix to both files (opacity ramp at 0%), ran Prettier formatting
- Session 5: PR reviewer rejected incremental approach, provided full rework plan
