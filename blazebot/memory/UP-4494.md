# Session Memory — UP-4494

## Progress
- Replaced SVG mask-based spotlight with CSS box-shadow approach in Spotlight.tsx
- Replaced CSS `inset` shorthand with individual `top`/`right`/`bottom`/`left` properties in PulsingRing.tsx keyframe animations
- Committed changes on branch blazebot/up-4494

## Decisions Made
- **Box-shadow approach over SVG fixes**: Replaced the entire SVG mask with CSS `box-shadow: 0 0 0 9999px` — eliminates Safari's percentage-in-SVG-mask bug entirely
- **Individual inset properties**: Safari cannot interpolate the `inset` shorthand in `@keyframes`, so expanded to `top`/`right`/`bottom`/`left`
- Could not run `yarn check` due to private registry auth (GITLAB_UNIFY_FRONTEND_TOKEN missing) — changes are pure CSS property swaps that don't affect types

## Blockers
- None

## Files Touched
- genai-engine/ui/src/features/tour/react/primitives/Spotlight.tsx
- genai-engine/ui/src/features/task-tour/components/PulsingRing.tsx

## Prior Sessions
- Research session identified the two root causes and produced the implementation plan
