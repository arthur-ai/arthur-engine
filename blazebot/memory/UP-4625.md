# Session Memory — UP-4625

## Progress
- IMPLEMENTED. PII (and all ML eval) annotation cost now reports zero / renders "N/A" instead of misleading "< $0.000001".
- Step 1: `AgenticAnnotation.from_db_model` (`src/schemas/internal_schemas.py`) now populates `eval_type` from `db_annotation.continuous_eval.eval_type` instead of hardcoding `None`. Added `eval_type = None` initializer and `eval_type = ce.eval_type` inside the continuous_eval block; changed constructor `eval_type=None` -> `eval_type=eval_type`. This makes the API tag ML annotations as `"ml_eval"`, triggering the existing FE "N/A" branch.
- Step 2: `_score_to_response` (`src/scorer/ml_scorers.py`) changed `cost=""` -> `cost="0"` so ML annotations persist `cost=0.0` (queue service does `float(cost) if cost else None`; "0" is truthy).
- Step 3/4: Created `tests/unit/schemas/test_internal_schemas.py` with 4 tests (ml_eval/llm_eval/human eval_type population + ml zero-cost). All pass. Existing `tests/unit/test_pii.py` still passes (72).

## Decisions Made
- EvalType enum members are `LLM_EVAL`/`ML_EVAL` (values "llm_eval"/"ml_eval"); tests assert against `EvalType.ML_EVAL`/`EvalType.LLM_EVAL`.
- Environment had no uv/venv — installed uv via astral installer, `uv sync --group dev --group linters`. Tests require `GENAI_ENGINE_SECRET_STORE_KEY` env var set.

## Blockers
- None.

## Files Touched
- `genai-engine/src/schemas/internal_schemas.py` (modified)
- `genai-engine/src/scorer/ml_scorers.py` (modified)
- `genai-engine/tests/unit/schemas/test_internal_schemas.py` (new)

## Prior Sessions
- Research phase: diagnosed root cause (FE already renders N/A for ml_eval but BE hardcoded eval_type=None, introduced in PR #1567). No code changes in that session.
