# Session Memory — UP-4091

## Progress
- Implemented the fix: modified the "Apply Filters" button onClick handler to auto-commit pending ID inputs before form submission
- The fix calls `handleAddId` for each non-empty input (traceIdInput, sessionIdInput, spanIdInput, userIdInput) before `form.handleSubmit()`
- Could not run `yarn check` due to missing GITLAB_UNIFY_FRONTEND_TOKEN for private registry auth

## Decisions Made
- Used the exact approach from the plan: commit pending inputs in the Apply Filters onClick handler
- TanStack Form's `setFieldValue` (called inside `handleAddId`) is synchronous, so values are available when `onSubmit` fires
- `handleAddId` already handles trimming, adding to form array, and clearing the input state

## Blockers
- None

## Files Touched
- `genai-engine/ui/src/components/traces/components/tables/components/TracingFilterModal.tsx` — modified Apply Filters onClick handler

## Prior Sessions
- Research phase identified root cause and fix location
